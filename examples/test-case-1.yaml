name: test-case-1
operations:
  # - action: apply
  #   template: template-1
  #   templateValues:
  #     namespace: default
  #     name: pod-name
  #   assert: |
  #     data.error == "";
  #     len(data.error) == 0;

  - action: get
    apiVersion: v1
    kind: Pod
    namespace: default
    name: pod-namesss
    retry: 10
    labelSelector: ""
    assert: |
      data.error == "";

  # - action: delete
  #   template: template-1
  #   templateValues:
  #     namespace: default
  #     name: pod-name
  #   assert: |
  #     data.error == "";
  #     len(data.error) == 0;
  # len(data.objects) == 1;
  # data.objects[0]["metadata"]["name"] == "pod-name";
  # data.error == "";
