description: test used as example
operations:
  - action: apply
    teardown: true/false
    template: template-1
    templateValues:
      namespace: default
      name: pod-name
    assert: |
      data.error;

  - action: exec
    name: pod-name
    namespace: default
    command: ["wrongcommand"]
    assert: |
      data.error;

  - action: get
    apiVersion: v1
    kind: Pod
    namespace: default
    name: pod-namesss
    retry: 10
    labelSelector: ""
    assert: |
      data.error == "";

  # - action: delete
  #   template: template-1
  #   templateValues:
  #     namespace: default
  #     name: pod-name
  #   assert: |
  #     data.error == "";
  #     len(data.error) == 0;
  # len(data.objects) == 1;
  # data.objects[0]["metadata"]["name"] == "pod-name";
  # data.error == "";
